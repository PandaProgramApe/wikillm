# WikiLLM - 基于LLM的知识库编译系统

# 核心依赖
flask>=3.0.0
pymupdf>=1.24.0; platform_system != "darwin" or platform_machine != "arm64"
pymupdf>=1.25.0; platform_system == "darwin" and platform_machine == "arm64"
python-docx>=1.1.0
markdown>=3.6
pyyaml>=6.0
python-dotenv>=1.0.0

# LLM支持
openai>=1.0.0
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0

# Ollama支持（本地模型）
requests>=2.31.0

# 可选：高级功能
tiktoken>=0.7.0  # token计数
chardet>=5.2.0    # 编码检测
